---
phase: 17
plan: 1
wave: 1
---

# Plan 17.1: Reporting Infrastructure & Tax Data Refactoring

## Objective
Standardize the reporting logic by moving heavy computations from UI components to a dedicated `reportService` and refactor `TaxDashboard.tsx` to use these services.

## Context
- .gsd/SPEC.md
- src/features/reports/TaxDashboard.tsx
- src/features/reports/hooks/useTaxData.ts

## Tasks

<task type="auto">
  <name>Create Report Service</name>
  <files>src/features/reports/services/reportService.ts</files>
  <action>
    Create the `services` directory in `src/features/reports`.
    Implement `reportService.ts` with methods for:
    - Calculating VAT sums (Vatable, Exempt, Zero-Rated).
    - Calculating Net and Gross Sales/Purchases for a period.
    - Preparing BIR-compliant summary data.
  </action>
  <verify>ls src/features/reports/services/reportService.ts</verify>
  <done>Report business logic is centralized and testable.</done>
</task>

<task type="auto">
  <name>Refactor useTaxData Hook</name>
  <files>src/features/reports/hooks/useTaxData.ts</files>
  <action>
    Update `useTaxData` to utilize the `reportService` for data transformation.
    Ensure query keys are consistent with the global `queryKeys` pattern.
    Add memoized calculations for metrics inside the hook or via service calls.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Hook returns pre-calculated metrics instead of just raw data.</done>
</task>

<task type="auto">
  <name>Standardize TaxDashboard UI</name>
  <files>src/features/reports/TaxDashboard.tsx</files>
  <action>
    Remove local `useMemo` blocks for metrics calculation.
    Use normalized data from the refactored `useTaxData` hook.
    Ensure formatting uses `src/shared/lib/formatUtils.ts`.
  </action>
  <verify>Check TaxDashboard in browser</verify>
  <done>UI is cleaner and follows standard data access patterns.</done>
</task>

## Success Criteria
- [ ] No heavy computation logic in `TaxDashboard.tsx`.
- [ ] BIR calculations centralized in `reportService.ts`.
- [ ] Successful compilation with zero TypeScript errors in the reports module.
