---
phase: 1
plan: 2
wave: 1
---

# Plan 1.2: Resolve React Hook Dependency Warnings

## Objective
Address unoptimized memoization and missing dependency arrays in React Hooks across feature modules. This prevents stale closures and optimizes performance. Address `prefer-const` warnings along the way.

## Context
- .gsd/phases/1/RESEARCH.md

## Tasks

<task type="auto">
  <name>Resolve Exhaustive Deps (Inventory & Dashboard & Purchasing)</name>
  <files>
    - src/features/customers/Customers.tsx
    - src/features/dashboard/DailySalesSummary.tsx
    - src/features/expenses/Expenses.tsx
    - src/features/expenses/components/ExpenseModal.tsx
    - src/features/inventory/AdminPricelist.tsx
    - src/features/inventory/BranchInventory.tsx
    - src/features/inventory/BranchInventoryDetail.tsx
    - src/features/inventory/DailyInventorySummary.tsx
    - src/features/inventory/Inventory.tsx
    - src/features/inventory/components/ProductModal.tsx
    - src/features/purchases/Returns.tsx
  </files>
  <action>
    - Apply suggested fixes for `react-hooks/exhaustive-deps`.
    - If variables are missing from dependency arrays, add them.
    - If functions are missing from dependency arrays, memoize them using `useCallback` where they are defined, OR decouple them from the array if safely possible.
    - Fix `prefer-const` warnings globally in `src/features/inventory/...` and `src/features/purchases/...` forms.
  </action>
  <verify>npm run lint</verify>
  <done>Zero `exhaustive-deps` warnings for Dashboard, Inventory, and Purchasing modules.</done>
</task>

<task type="auto">
  <name>Resolve Exhaustive Deps (Sales Modules) & Incompatible API</name>
  <files>
    - src/features/purchases/components/PurchaseModal.tsx
    - src/features/purchases/components/ReturnModal.tsx
    - src/features/sales/ExpressSales.tsx
    - src/features/sales/components/RefundModal.tsx
    - src/features/sales/components/SalesModal.tsx
    - src/features/suppliers/components/SupplierModal.tsx
    - src/shared/lib/AuthContext.tsx
  </files>
  <action>
    - Address `react-hooks/exhaustive-deps` in Sales features and remaining modals/contexts.
    - Specifically, in `SupplierModal.tsx` deal with `react-hooks/incompatible-library` related to `watch()` from `react-hook-form` by ensuring values watched are correctly managed. 
  </action>
  <verify>npm run lint</verify>
  <done>All `exhaustive-deps` and plugin-specific warnings (`prefer-const`, `incompatible-library`) are cleared.</done>
</task>

## Success Criteria
- [ ] 0 `react-hooks/exhaustive-deps` warnings app-wide.
- [ ] 0 `react-hooks/incompatible-library` warnings.
