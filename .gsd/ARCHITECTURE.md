# Architecture

> Auto-generated by /map

## Overview

EMC Retail OS is a frontend-heavy web application (React + Vite) designed to manage retail operations including point of sale, inventory, purchasing, customer management, and dashboard analytics. The application utilizes Supabase as its backend-as-a-service (BaaS) for direct integration encompassing database operations and authentication.

```text
┌─────────────────────────────────────────┐
│        [Entry Point: src/main.tsx]      │
├─────────────────────────────────────────┤
│         [Business Logic Layer]          │
│   src/features/* (sales, inventory)     │
├─────────────────────────────────────────┤
│            [Data Layer]                 │
│         Supabase (PostgreSQL)           │
└─────────────────────────────────────────┘
```

## Components

### Features (`src/features/`)
- **Purpose:** Encapsulate domain-specific logic, UI components, and services.
- **Location:** `src/features/*` 
  - Modules discovered: `auth`, `customers`, `dashboard`, `expenses`, `home`, `inventory`, `purchases`, `reports`, `sales`, `suppliers`.
- **Dependencies:** Leverages `@tanstack/react-query` for data fetching/mutability and `supabase` client for backend interaction.

### Shared (`src/shared/`)
- **Purpose:** Cross-domain functionality like hooks, generic UI components, and context providers.
- **Location:** `src/shared/`
- **Dependencies:** React Context (`AuthContext`, `BranchContext`), Supabase client.

## Data Flow

1. **User Interaction**: User interacts with a specific feature component (e.g., `SalesModal.tsx`).
2. **State & Fetching**: Uses `@tanstack/react-query` to interact with services (e.g., `salesService.ts`, `productService.ts`).
3. **Data Layer**: The service invokes `@supabase/supabase-js` to read/write directly to the Supabase database.
4. **Context Update**: Global state such as Authentication and Branch context provides the necessary session/tenant isolation.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Supabase | BaaS | Authentication, Database Querying, and Realtime subscriptions |
| API | External | `fetch` API utilized lightly (detected in `Returns.tsx`) |

## Technical Debt

- [ ] **Lack of Automated Tests**: No testing framework (e.g., Jest, Vitest) detected in `package.json` or visible `tests/` directories.
- [ ] **Error Handling Gaps**: No explicit, app-wide Error Boundaries or structured error logging mechanisms detected (e.g., Sentry).

## Conventions

**Naming:** React components are PascalCase. Services, hooks, and utilities are camelCase.
**Structure:** Feature-driven architecture where each domain manages its own services and UI components.
**Testing:** No automated testing structures currently in place.
