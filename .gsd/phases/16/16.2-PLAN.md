---
phase: 16
plan: 2
wave: 2
---

# Plan 16.2: Logic & Pattern Standardization (React Query/Supabase)

## Objective
Refactor business logic out of components and standardize how the application interacts with Supabase and React Query. This addresses REQ-01, REQ-04, and REQ-05.

## Context
- .gsd/REQUIREMENTS.md
- src/shared/lib/supabase.ts (if exists)
- src/shared/hooks/
- src/features/

## Tasks

<task type="auto">
  <name>Supabase Client Standardization</name>
  <files>src/shared/lib/supabase.ts, src/**/*</files>
  <action>
    Ensure a single, consistent entry point for the Supabase client.
    Audit all files for ad-hoc `createClient` calls and replace them with the shared instance.
  </action>
  <verify>grep -r "createClient" src</verify>
  <done>Unified Supabase client initialization.</done>
</task>

<task type="auto">
  <name>React Query Audit</name>
  <files>src/shared/hooks/**/*, src/features/**/*</files>
  <action>
    Standardize query keys and stale times.
    Implement custom hooks for all data fetching/mutations instead of calling `useQuery` or `useMutation` directly in components.
    Ensure proper error handling and loading states.
  </action>
  <verify>Manual review of query keys and hook architecture</verify>
  <done>Standardized React Query patterns and custom hooks for all data operations.</done>
</task>

<task type="auto">
  <name>Business Logic Refactoring</name>
  <files>src/features/transfers/**/*, src/features/inventory/**/*</files>
  <action>
    Move complex logic (stock calculations, transfer state transitions) from component files into dedicated services or custom hooks.
    Ensure components are primarily responsible for rendering UI.
  </action>
  <verify>Manual review of component vs hook logic</verify>
  <done>Clean separation of concerns between UI and business logic.</done>
</task>

## Success Criteria
- [ ] All data interactions use standardized custom hooks.
- [ ] Components are lean and UI-focused.
- [ ] Supabase initialization is centralized.
